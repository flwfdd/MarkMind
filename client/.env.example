# Example Vite environment variables for the client
# Copy this to .env (or .env.local) and adjust as necessary

# Base URL for the backend API (include the /api suffix if desired)
VITE_API_BASE=http://127.0.0.1:8080/api
